# Sample Repo

This repo exists solely to generate GitHub Actions logs for the CI Log Analyzer project.

It has two jobs:
- `build-and-test` — passes
- `failing-job` — intentionally fails (ModuleNotFoundError)
# trigger analysis
